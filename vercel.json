{
  "buildCommand": "npm install && npx preconstruct build && npm run build --workspace=@gephi/gephi-lite",
  "outputDirectory": "packages/gephi-lite/build",
  "functions": {
    "api/**/*.ts": {
      "runtime": "nodejs18.x"
    }
  }
}
