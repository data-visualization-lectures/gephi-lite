{
  "buildCommand": "npm install --legacy-peer-deps && npx @preconstruct/cli@^2.8.10 build --filter=packages/sdk --filter=packages/broadcast && npm run build --workspace=@gephi/gephi-lite",
  "outputDirectory": "packages/gephi-lite/build",
  "functions": {
    "api/**/*.ts": {
      "runtime": "nodejs18.x"
    }
  }
}
