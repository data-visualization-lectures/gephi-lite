{
  "buildCommand": "npm install --legacy-peer-deps && npx preconstruct dev && npm run build",
  "outputDirectory": "packages/gephi-lite/build",
  "functions": {
    "api/**/*.ts": {
      "runtime": "nodejs18.x"
    }
  }
}
